# Git Workflow для FTC Команды 25444

## 📋 Структура веток

- **`main`** - главная ветка, только проверенный и одобренный код
- **`dev`** - ветка разработки для тестирования перед merge в main
- **`programmer1`** - персональная ветка программиста 1
- **`programmer2`** - персональная ветка программиста 2
- **`programmer3`** - персональная ветка программиста 3
- **`programmer4`** - персональная ветка программиста 4

## 🔄 Рабочий процесс (Workflow)

### 1. Создать свою ветку от main:
```bash
git checkout main
git pull
git checkout -b programmer1  # замените на ваше имя
```

### 2. Работать в своей ветке:
```bash
# Делаете изменения в коде
git add .
git commit -m "Описание изменений"
```

### 3. Подготовить код для review:
```bash
# Переключиться на main и обновить
git checkout main
git pull

# Вернуться в свою ветку и объединить изменения из main
git checkout programmer1
git merge main

# Если есть конфликты - решить их
```

### 4. Отправить на review (GitHub Pull Request):
```bash
# Отправить свою ветку на GitHub
git push origin programmer1

# Затем на GitHub:
# - Открыть Pull Request: programmer1 → main
# - Дождаться одобрения от лидера команды
# - После одобрения ваш код будет добавлен в main
```

## 📝 Полезные команды

**Посмотреть текущую ветку:**
```bash
git branch
```

**Посмотреть статус (что изменилось):**
```bash
git status
```

**Посмотреть свои изменения:**
```bash
git diff
```

**Посмотреть историю коммитов:**
```bash
git log --oneline --graph --decorate
```

**Обновить свою ветку из main:**
```bash
git checkout main
git pull
git checkout ваша_ветка
git merge main
```

## 🚨 Откатить нежелательный Pull Request

Если кто-то случайно смержил свой Pull Request в main без одобрения, вот как это исправить:

### Шаг 1: Найти хеш "хорошего" коммита
```bash
# Посмотреть историю коммитов
git log --oneline -10

# Найдите последний правильный коммит (до нежелательного merge)
```

### Шаг 2: Откатить main к "хорошему" коммиту
```bash
# Переключиться на main
git checkout main

# Откатить к нужному коммиту (замените ХХХХХХ на хеш коммита)
git reset --hard ХХХХХХ

# Принудительно запушить на GitHub
git push origin main --force
```

⚠️ **Внимание:** Команда `--force` удалит все коммиты после указанного! Используйте осторожно!

💡 **Совет:** Всегда делайте резервную копию перед использованием `--force`:
```bash
# Создать резервную ветку на всякий случай
git branch backup-main
```


