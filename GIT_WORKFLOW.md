# Git Workflow для FTC Команды 25444

## 📋 Структура веток

- **`main`** - главная ветка, только проверенный и одобренный код
- **`dev`** - ветка разработки для тестирования перед merge в main
- **`programmer1`** - персональная ветка программиста 1
- **`programmer2`** - персональная ветка программиста 2
- **`programmer3`** - персональная ветка программиста 3
- **`programmer4`** - персональная ветка программиста 4

## 🔄 Рабочий процесс (Workflow)

### Шаг 1: Создать свою ветку
```bash
git checkout -b programmer1  # замените на ваше имя (например: alibek, rustam)
```

### Шаг 2: Работать и сохранять изменения
```bash
# После того как изменили код:
git add .
git commit -m "Описание что сделали"
```
💡 Совет: можно делать много коммитов! Каждый раз когда сделали что-то - сохраняйте.

### Шаг 3: Отправить на GitHub для review
```bash
# Отправить свою ветку на GitHub
git push origin programmer1  # замените на имя ВАШЕЙ ветки
```

### Шаг 4: Создать Pull Request на GitHub
1. Зайдите на https://github.com/Makhkambek/FTC-25444-Code
2. GitHub автоматически покажет кнопку **"Compare & pull request"** - нажмите её
3. Напишите что вы сделали
4. Нажмите **"Create Pull Request"**
5. Дождитесь одобрения от Makhkambek
6. После одобрения - код попадет в main! ✅

## 📝 Полезные команды

**Посмотреть в какой ветке вы сейчас:**
```bash
git branch
```

**Посмотреть что изменили (перед коммитом):**
```bash
git status
```

**Посмотреть изменения в коде:**
```bash
git diff
```

## ❗ Если что-то пошло не так

**Отменить все изменения (вернуться к последнему коммиту):**
```bash
git checkout .
```

**Переключиться на другую ветку:**
```bash
git checkout имя_ветки
```


