# GitHub Actions Workflows

Этот проект использует GitHub Actions для автоматизации проверки кода.

## 📋 Установленные Workflows

### 1. **PR Code Check** (`pr-check.yml`)
**Триггер:** При создании или обновлении Pull Request в ветки `main` или `dev`

**Что делает:**
- ✅ Проверяет наличие Java файлов
- ✅ Проверяет базовый синтаксис Java
- ✅ Ищет незавершенный код (например, `speedM` без завершения)
- ✅ Добавляет комментарий в PR если находит ошибки

**Важно:** ❌ НЕ одобряет и НЕ мерджит PR автоматически!

---

### 2. **Create Issue on Error** (`issue-on-error.yml`)
**Триггер:** 
- Push в ветки `dev` или `feature/**`
- Pull Request в ветки `main` или `dev`

**Что делает:**
- 🔍 Сканирует код на наличие проблем
- 🐛 Автоматически создает Issue если находит:
  - Незавершенные строки кода
  - Неопределенные переменные (например, `strafe`)
- 📝 Добавляет метки `automated` и `bug`

---

### 3. **PR Review Comments** (`pr-review-comment.yml`)
**Триггер:** При создании или обновлении Pull Request

**Что делает:**
- 💬 Оставляет автоматический комментарий с напоминаниями:
  - Проверить ошибки компиляции
  - Протестировать код на роботе
  - Убедиться что нет незавершенных строк

**Важно:** Использует `event: 'COMMENT'` - это значит комментарий БЕЗ одобрения!

---

## 🔒 Безопасность

Все workflows настроены так, что они:
- ✅ **ТОЛЬКО проверяют** код и оставляют комментарии
- ❌ **НЕ одобряют** Pull Request автоматически
- ❌ **НЕ мерджат** код в main или dev
- ✅ **Вы сами принимаете решение** когда мерджить

---

## 🚀 Как использовать

1. **Создайте Pull Request** - workflows запустятся автоматически
2. **Посмотрите результаты** в разделе "Checks" вашего PR
3. **Прочитайте комментарии** если есть проблемы
4. **Исправьте код** если нужно
5. **Одобрите и смерджите вручную** когда все готово

---

## 📊 Статусы проверок

- ✅ **Green check** - все проверки прошли успешно
- ❌ **Red X** - найдены проблемы, нужно исправить
- 🟡 **Yellow dot** - проверка в процессе

---

## 🛠️ Настройка

Чтобы workflows заработали в вашем репозитории:

1. Убедитесь что в Settings → Actions → General:
   - "Allow all actions and reusable workflows" включено
   - "Read and write permissions" для GITHUB_TOKEN включено

2. Files добавлены в репозиторий:
   ```
   .github/
   └── workflows/
       ├── pr-check.yml
       ├── issue-on-error.yml
       └── pr-review-comment.yml
   ```

3. Закоммитьте и запушьте в GitHub:
   ```bash
   git add .github/
   git commit -m "Add GitHub Actions workflows"
   git push
   ```

---

## 📝 Примечания

- Workflows используют Java 11 (можно изменить в файле)
- Проверки адаптированы для FTC кода
- Можно добавить больше проверок по необходимости

**Вопросы?** Проверьте вкладку "Actions" в вашем GitHub репозитории для логов выполнения.
